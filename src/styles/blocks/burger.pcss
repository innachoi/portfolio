.burger-menu {
  display: none;

  @include tablets {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
  }
}

.burger-menu div {
  width: 30px;
  height: 5px;
  background: white;
}